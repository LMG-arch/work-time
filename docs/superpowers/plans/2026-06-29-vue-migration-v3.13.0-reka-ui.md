# v3.13.0: Reka UI 全面集成 + UI 美化

**Goal:** 用 Reka UI headless 组件替换原生元素，统一视觉风格

**Scope:**
- Dialog → 替换 TodoModal, ReminderSettings, PostModal
- Select → 替换 todo remind/日历选择等原生 `<select>`
- Collapsible → 替换设置页折叠面板
- CSS 统一美化（配色、间距、圆角、阴影、过渡动画）

**不做：**
- 功能变更
- Calendar Reka UI Calendar 组件替换（范围太大，得不偿失）
- Avatar/Combobox 等复杂组件

---

### Task 1: Dialog 替换 TodoModal

### Task 2: Dialog 替换 ReminderSettings

### Task 3: Select 替换原生下拉框

### Task 4: Collapsible 替换设置折叠

### Task 5: CSS 全面美化

### Task 6: 版本发布 v3.13.0
